import 'package:flutter/material.dart';

class Constants {
  static const defaultTitleBarPagePadding = EdgeInsets.symmetric(
    vertical: 20,
    horizontal: 22,
  );
  static const defaultHeaderLeadingWidgetWidth = 40.0;
  static const defaultHeaderSize = Size.fromHeight(40);
}
